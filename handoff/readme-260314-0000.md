# Handoff - chunking-english - 2026-03-14 00:00

## Completed
- Ran reimport_full.py locally: 749 verbs, 5268 expressions for all 250 days (0 skipped)
  - Source: asset/img_manifest.json (paths) + asset/kr_mapping.json (Korean translations)
  - I_ images → verb.sentence_en; regular images → expressions with image_path
- Updated book.php Listen & Repeat section to use data/10words_mapping.json (10 sentences per day)
  - Added PHP loading block (data/10words_mapping.json → $lr_items)
  - Added .chunk-type-label CSS
  - Replaced DB expressions loop with 10words items showing chunk_type, en_front+en_back, kr_front+kr_back
- Verified locally: 10words loads correctly (Day1: 10 sentences, correct Korean/English)
- Synced book.php + data/10words_mapping.json to htdocs

## Remaining
- GCP Cloud SQL re-import: reimport_full.py logic needs to run on GCP (admin/reimport.php uses old final_mapping approach, not manifest+kr_mapping approach)
- GCP deploy: book.php + data/10words_mapping.json need to be deployed
- Image sync on GCP: after DB re-import, run sync to set image_path for all expressions (expect ~100% coverage)
- verb_kr is empty in DB (verbs table verb_kr column blank) — may need 10words or other source
- Cleanup: admin/reimport.php and debug_manifest.php should be removed/protected on GCP

## Next Steps
1. Create a new GCP-compatible reimport endpoint (admin/reimport_v2.php) that uses data/img_manifest.json + data/kr_mapping.json (same logic as reimport_full.py)
2. Deploy to GCP: `cd C:/work/chunking-english && gcloud app deploy --quiet`
3. Run admin/reimport_v2.php on GCP via browser
4. Run image sync on GCP to set image_path
5. Verify book.php on GCP shows correct 10words + verb expressions with images

## Context
- Local: C:/work/chunking-english + C:/xampp/htdocs/chunking-english (must sync both)
- GCP: https://chunking-english-489506.du.r.appspot.com
- Admin login: 260310 / 260310
- data/10words_mapping.json: 250 days × 10 sentences (chunk_type, en_front, en_back, kr_front, kr_back)
- data/kr_mapping.json: 5638 slug→Korean translation mappings
- data/img_manifest.json: 6025 image paths (already on GCP)
- reimport_full.py: local Python script, 749 verbs/5268 expressions from manifest+kr_mapping
- Current GCP DB: still has old data (reimport_full.py only ran locally)
- GCP deploys occasionally timeout — retry 2-3 times
