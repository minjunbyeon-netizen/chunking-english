# Handoff - chunking-english - 2026-03-12 13:55

## Completed
- Compared desktop `checek` folder (designer's mockup) against `C:/work/chunking-english`
- Applied designer CSS from checek/css/style.css to work/css/style.css (design-only, no logic touched)
- Copied 3 new images: au01.png, au02.png, au03.png to work/img/
- Synced style.css and new images to htdocs

## CSS Changes Applied from Designer
- `#board-section`: added `padding: 100px 180px` and `box-sizing: border-box`
- `#board-section iframe`: replaced `border: none` with pink rounded frame design (border-radius 24px, border 4px solid #FFD1DA, box-shadow)
- Added: `body.is-in-board #floating-board-btn` visibility control block
- Added: Tablet (max-width: 1200px) responsive rules for #board-section and map layout
- Added: Tablet logo resize (768px ~ 1200px): logo-layer, logo-line--primary/secondary, logo-kids-mom, logo-stage
- Added: Mobile (767px) together-fixed scale: 0.75
- Added: Mobile (768px) board-section with iframe border styling
- Added: Mobile (480px) tiny screen board-section
- Added: PC (1201px+) board-section full-screen override (padding: 0, border: none)
- Added: Mobile (767px) logo-scrolled hide

## Kept from Work (NOT overwritten)
- `white-space: nowrap; overflow: hidden; text-overflow: ellipsis` on .station-ride (CLAUDE.md: no text wrapping)
- `@keyframes mini-card-flip` and `.mini-card.flipping` (newer feature in work)
- Logout button styles (.logout-fixed, .logout-btn, etc.)
- All PHP backend logic (session, DB, auth) — untouched

## Remaining
- PHP file HTML structure differences (board.php view-modal, login.php CSS formatting) — not applied yet, needs careful review

## Next Steps
- Visual test on localhost: check board-section pink frame on desktop vs full-screen on PC 1201px+
- Verify tablet (768-1200px) map layout looks correct
- Confirm au01~03.png images are actually used somewhere in the codebase (or if they need to be wired up)

## Context
- checek = designer's static mockup based on older version of the codebase (no PHP backend)
- work = the real production code with DB/auth/session logic
- Only pure CSS changes from checek were merged; all logic remained from work
